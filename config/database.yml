# MySQL. Versions 5.0 and up are supported.
#
# Install the MySQL driver
#   gem install mysql2
#
# Ensure the MySQL gem is defined in your Gemfile
#   gem 'mysql2'
#
# And be sure to use new-style password hashing:
#   http://dev.mysql.com/doc/refman/5.7/en/old-client.html
#
default: &default
  adapter: mysql2
  encoding: utf8
  pool: 5
  username: <%= ENV['DIOS_DATABASE_USER'] || "root" %>
  password: <%= ENV['DIOS_DATABASE_PASSWORD'] || "" %>
  host: <%= ENV['DIOS_DATABASE_HOST'] || "127.0.0.1" %>
  port: <%= ENV['DIOS_DATABASE_PORT'] || "3306" %>

development:
  <<: *default
  database: dios_development

test:
  <<: *default
  database: dios_test

production:
  <<: *default
  database: dios_production

default_cuenta: &default_cuenta
  adapter: mysql2
  encoding: utf8
  pool: 5
  username: <%= ENV['CUENTA_DATABASE_USER'] || "root" %>
  password: <%= ENV['CUENTA_DATABASE_PASSWORD'] || "" %>
  host: <%= ENV['CUENTA_DATABASE_HOST'] || "127.0.0.1" %>
  port: <%= ENV['CUENTA_DATABASE_PORT'] || "3306" %>

development_cuenta:
  <<: *default_cuenta
  database: cuenta_development

test_cuenta:
  <<: *default_cuenta
  database: cuenta_test

production_cuenta:
  <<: *default_cuenta
  database: cuenta_production

default_aldea: &default_aldea
  adapter: mysql2
  encoding: utf8
  pool: 5
  username: <%= ENV['ALDEA_DATABASE_USER'] || "root" %>
  password: <%= ENV['ALDEA_DATABASE_PASSWORD'] || "" %>
  host: <%= ENV['ALDEA_DATABASE_HOST'] || "127.0.0.1" %>
  port: <%= ENV['ALDEA_DATABASE_PORT'] || "3306" %>

development_aldea:
  <<: *default_aldea
  database: aldea_development

test_aldea:
  <<: *default_aldea
  database: aldea_test

production_aldea:
  <<: *default_aldea
  database: aldea_production
